name: Theme Releases
run-name: 🚀 ${{ github.event.release.tag_name }} - ${{ github.actor }}
on:
  release:
    types: [published]
permissions:
  contents: write
jobs:
  Create-Theme-Releases:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Create themes
        run: |
          cd ${{ github.workspace }}/theme
          for dir in */ ; do
            cd $dir
            convert ./preview.png -resize 288x216 ./preview.png
            convert ./image/bootlogo.png ./image/bootlogo.bmp
            rm -rf ./image/bootlogo.png
            echo "${dir%/} (${{ github.event.release.tag_name }})" > ./theme_name.txt
            cat ./theme_name.txt
            zip -r "../${dir%/}.zip" ./
            cd ..
          done
      - name: Upload releases
        run: |
          for file in ${{ github.workspace }}/theme/*.zip; do
            echo "Uploading $file"
            gh release upload ${{ github.event.release.tag_name }} "$file" --clobber
          done
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
